apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sentiment-frontend-ingress
  namespace: sentiment-app-dev
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx
  rules:
    - host: sentiment-app.comdev
      http:
        paths:
          - path: /api(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: model-api-service
                port:
                  number: 8081
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sentiment-frontend-service
                port:
                  number: 8082
